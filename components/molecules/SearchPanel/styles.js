import styled from "styled-components";
import { PageHeader } from "antd";
import { LUP_BASE_GREY_LIGHTER } from "../../../theme";

export const StyledSearchPanel = styled(PageHeader)`
    padding: 4px 24px;
    border: 1px solid ${LUP_BASE_GREY_LIGHTER};
`;