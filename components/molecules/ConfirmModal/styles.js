import styled from "styled-components";
import { Block } from "../../atoms";
import { LUP_SPACING_30, LUP_SPACING_20, LUP_SPACING_24 } from '../../../theme';

const BodyWrapper = styled(Block)`
  padding: ${LUP_SPACING_30} ${LUP_SPACING_20} ${LUP_SPACING_24};
`;

export { BodyWrapper };
