import styled from "styled-components";
import { LUP_SPACING_2 } from "../../../theme";
import { Block } from '../../atoms';

const LabelWrapper = styled(Block)`
  margin: ${LUP_SPACING_2} 0;
`;

export { LabelWrapper };
